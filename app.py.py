import streamlit as st import sqlite3 import pandas as pd import hashlib import io import datetime # ============================================================================== # CONFIGURAÇÃO DA PÁGINA # ============================================================================== st.set\_page\_config( page\_title="Sistema de Gestão de Pessoal - Supera", page\_icon="👥", layout="wide", initial\_sidebar\_state="expanded" ) DB\_FILE = "supera\_rh.db" # ============================================================================== # BANCO DE DADOS &amp; AUTENTICAÇÃO # ============================================================================== def get\_connection(): return sqlite3.connect(DB\_FILE) def make\_hashes(password): return hashlib.sha256(str.encode(password)).hexdigest() def check\_hashes(password, hashed\_text): if make\_hashes(password) == hashed\_text: return hashed\_text return False def init\_db(): conn = get\_connection() c = conn.cursor() # Tabela de Usuários c.execute(''' CREATE TABLE IF NOT EXISTS usuarios ( id INTEGER PRIMARY KEY AUTOINCREMENT, nome TEXT NOT NULL, email TEXT UNIQUE NOT NULL, senha\_hash TEXT NOT NULL, perfil TEXT NOT NULL, unidade TEXT DEFAULT 'Todas' ) ''') # Tabela de Colaboradores c.execute(''' CREATE TABLE IF NOT EXISTS colaboradores ( id INTEGER PRIMARY KEY AUTOINCREMENT, funcional TEXT UNIQUE, nome TEXT NOT NULL, cpf TEXT, data\_nascimento TEXT, sexo TEXT, naturalidade TEXT, nome\_pai TEXT, nome\_mae TEXT, cep TEXT, endereco TEXT, numero TEXT, complemento TEXT, bairro TEXT, cidade TEXT, uf TEXT, rg TEXT, orgao\_emissor TEXT, ctps TEXT, pis TEXT, data\_admissao TEXT, cod\_funcao TEXT, funcao TEXT, filial TEXT NOT NULL, status TEXT NOT NULL DEFAULT 'Ativo', carga\_horaria TEXT, telefone TEXT, email TEXT, salario\_base REAL, depend\_irpf INTEGER DEFAULT 0, depend\_salario INTEGER DEFAULT 0, banco TEXT, agencia TEXT, conta TEXT, chave\_pix TEXT, vale\_transporte TEXT DEFAULT 'Não', assistencia\_medica TEXT DEFAULT 'Não' ) ''') # Inserir usuários padrão se não existirem c.execute("SELECT COUNT(\*) FROM usuarios") if c.fetchone() == 0: usuarios\_default = [ ("Administrador RH", "admin@supera.com", make\_hashes("admin123"), "Administrador", "Todas"), ("Gestor Barueri", "editor.barueri@supera.com", make\_hashes("editor123"), "Editor", "Hospital Barueri / Supera"), ("Consulta Operacional", "consulta@supera.com", make\_hashes("user123"), "Visualizador", "Todas") ] c.executemany("INSERT INTO usuarios (nome, email, senha\_hash, perfil, unidade) VALUES (?,?,?,?,?)", usuarios\_default) conn.commit() conn.close() def authenticate\_user(email, password): conn = get\_connection() c = conn.cursor() c.execute("SELECT id, nome, email, perfil, unidade, senha\_hash FROM usuarios WHERE email = ?", (email,)) user\_data = c.fetchone() conn.close() if user\_data: if check\_hashes(password, user\_data[1]): return { "id": user\_data, "nome": user\_data[2], "email": user\_data[3], "perfil": user\_data[4], "unidade": user\_data[5] } return None def mask\_cpf(cpf): if not cpf or len(str(cpf).strip()) &lt; 11: return "\*\*\*.\*\*\*.\*\*\*-\*\*" clean\_cpf = ''.join(filter(str.isdigit, str(cpf))) if len(clean\_cpf) == 11: return f"\*\*\*.\*\*\*.{clean\_cpf[6:9]}-{clean\_cpf[9:11]}" return "\*\*\*.\*\*\*.\*\*\*-\*\*" def map\_dataframe\_columns(df): mapping = {} cols = df.columns for col in cols: c\_upper = str(col).upper().strip() if "NOME" in c\_upper and "PAI" not in c\_upper and "MAE" not in c\_upper: mapping[col] = "nome" elif "CPF" in c\_upper: mapping[col] = "cpf" elif "FUNCIONAL" in c\_upper or "MATRICULA" in c\_upper or "MATRÍCULA" in c\_upper: mapping[col] = "funcional" elif "FILIAL" in c\_upper or "UNIDADE" in c\_upper or "EMPRESA" in c\_upper: mapping[col] = "filial" elif "FUNCAO" in c\_upper or "FUNÇÃO" in c\_upper or "CARGO" in c\_upper: mapping[col] = "funcao" elif "SALARIO" in c\_upper or "SALÁRIO" in c\_upper: mapping[col] = "salario\_base" elif "STATUS" in c\_upper or "SITUAÇÃO" in c\_upper or "SITUACAO" in c\_upper: mapping[col] = "status" elif "ADMISSAO" in c\_upper or "ADMISSÃO" in c\_upper: mapping[col] = "data\_admissao" elif "CARGA" in c\_upper or "ESCALA" in c\_upper or "HORARIO" in c\_upper or "HORÁRIO" in c\_upper: mapping[col] = "carga\_horaria" elif "TELEFONE" in c\_upper or "CELULAR" in c\_upper: mapping[col] = "telefone" elif "EMAIL" in c\_upper or "E-MAIL" in c\_upper: mapping[col] = "email" elif "PIS" in c\_upper: mapping[col] = "pis" elif "RG" in c\_upper: mapping[col] = "rg" elif "BANCO" in c\_upper: mapping[col] = "banco" elif "AGENCIA" in c\_upper or "AGÊNCIA" in c\_upper: mapping[col] = "agencia" elif "CONTA" in c\_upper: mapping[col] = "conta" elif "PIX" in c\_upper: mapping[col] = "chave\_pix" return mapping # INICIALIZAÇÃO init\_db() if 'logged\_in' not in st.session\_state: st.session\_state['logged\_in'] = False if 'user' not in st.session\_state: st.session\_state['user'] = None # TELA DE LOGIN if not st.session\_state['logged\_in']: st.markdown("<h2>🔐 Sistema de Gestão e Registro de Pessoal</h2>", unsafe\_allow\_html=True) st.markdown("<h4>Supera Alimentação &amp; Serviços</h4>", unsafe\_allow\_html=True) st.divider() col1, col2, col3 = st.columns([2, 3]) with col2: st.subheader("Acesso ao Sistema") with st.form("login\_form"): email\_input = st.text\_input("E-mail do Usuário", value="admin@supera.com") password\_input = st.text\_input("Senha", type="password", value="admin123") submit\_login = st.form\_submit\_button("Entrar no Sistema", use\_container\_width=True) if submit\_login: user = authenticate\_user(email\_input, password\_input) if user: st.session\_state['logged\_in'] = True st.session\_state['user'] = user st.success(f"Bem-vindo(a), {user['nome']}!") st.rerun() else: st.error("E-mail ou senha incorretos. Tente novamente.") st.info(""" 💡 \*\*Credenciais de Teste:\*\* \* \*\*Administrador (RH Total)\*\*: \`admin@supera.com\` | \`admin123\` \* \*\*Editor (Gestor Barueri)\*\*: \`editor.barueri@supera.com\` | \`editor123\` \* \*\*Visualizador (Apenas Consulta)\*\*: \`consulta@supera.com\` | \`user123\` """) st.stop() current\_user = st.session\_state['user'] # MENU PRINCIPAL with st.sidebar: st.title("🏢 Supera RH Web") st.write(f"👤 \*\*{current\_user['nome']}\*\*") st.caption(f"Perfil: \*\*{current\_user['perfil']}\*\*") if current\_user['unidade'] != 'Todas': st.caption(f"Escopo: \`{current\_user['unidade']}\`") st.divider() menu = st.radio( "Navegação:", ["📊 Dashboard de RH", "👥 Cadastros de Colaboradores", "🔍 Pesquisa &amp; Relatórios", "⚙️ Gerenciar Usuários"] ) st.divider() if st.button("🚪 Sair do Sistema", use\_container\_width=True): st.session\_state['logged\_in'] = False st.session\_state['user'] = None st.rerun() def load\_colaboradores\_df(unidade\_filtro=None, status\_filtro=None): conn = get\_connection() query = "SELECT \* FROM colaboradores WHERE 1=1" params = [] if current\_user['perfil'] == 'Editor' and current\_user['unidade'] != 'Todas': query += " AND filial = ?" params.append(current\_user['unidade']) elif unidade\_filtro and unidade\_filtro != 'Todas': query += " AND filial = ?" params.append(unidade\_filtro) if status\_filtro and status\_filtro != 'Todos': query += " AND status = ?" params.append(status\_filtro) df = pd.read\_sql\_query(query, conn, params=params) conn.close() return df # MÓDULO 1: DASHBOARD if menu == "📊 Dashboard de RH": st.title("📊 Painel de Indicadores de Gestão de Pessoal") st.markdown("Visão consolidada da força de trabalho por filial, função e status contratual.") df\_all = load\_colaboradores\_df() c1, c2, c3, c4 = st.columns(4) total\_func = len(df\_all) total\_ativos = len(df\_all[df\_all['status'] == 'Ativo']) total\_demitidos = len(df\_all[df\_all['status'] == 'Demitido']) total\_afastados = len(df\_all[df\_all['status'].str.contains('Afastado', na=False)]) c1.metric("Total Cadastrados", total\_func) c2.metric("Ativos na Equipe", total\_ativos, delta=f"{int(total\_ativos/total\_func\*100) if total\_func&gt;0 else 0}% do total") c3.metric("Desligados / Demitidos", total\_demitidos) c4.metric("Afastados (INSS / Outros)", total\_afastados) st.divider() col\_g1, col\_g2 = st.columns(2) with col\_g1: st.subheader("🏢 Distribuição por Filial / Unidade") if not df\_all.empty: filial\_counts = df\_all['filial'].value\_counts().reset\_index() filial\_counts.columns = ['Filial', 'Quantidade'] st.bar\_chart(filial\_counts.set\_index('Filial')) else: st.info("Nenhum registro encontrado.") with col\_g2: st.subheader("👔 Principais Cargos / Funções") if not df\_all.empty: cargo\_counts = df\_all['funcao'].value\_counts().head(8).reset\_index() cargo\_counts.columns = ['Função', 'Quantidade'] st.dataframe(cargo\_counts, use\_container\_width=True, hide\_index=True) else: st.info("Nenhum registro encontrado.") st.subheader("📋 Resumo por Status Contratual") if not df\_all.empty: status\_df = df\_all.groupby(['filial', 'status']).size().unstack(fill\_value=0) st.dataframe(status\_df, use\_container\_width=True) # MÓDULO 2: CADASTROS (COM IMPORTAÇÃO) elif menu == "👥 Cadastros de Colaboradores": st.title("👥 Gestão de Colaboradores") tab\_list, tab\_novo, tab\_editar, tab\_import = st.tabs([ "📋 Listagem Geral", "➕ Adicionar Colaborador", "✏️ Atualizar / Editar Cadastro", "📥 Importar Planilha (Excel/CSV)" ]) with tab\_list: st.subheader("Colaboradores Cadastrados") f\_col1, f\_col2 = st.columns(2) with f\_col1: conn = get\_connection() unidades\_list = ["Todas"] + [u for u in conn.execute("SELECT DISTINCT filial FROM colaboradores").fetchall() if u] conn.close() sel\_unidade = st.selectbox("Filtrar por Unidade/Filial:", unidades\_list) with f\_col2: sel\_status = st.selectbox("Filtrar por Status:", ["Todos", "Ativo", "Demitido", "Afastado INSS"]) df\_display = load\_colaboradores\_df(sel\_unidade, sel\_status) if current\_user['perfil'] != 'Administrador': if 'salario\_base' in df\_display.columns: df\_display['salario\_base'] = None if 'banco' in df\_display.columns: df\_display['banco'] = "\*\*\*" if 'conta' in df\_display.columns: df\_display['conta'] = "\*\*\*\*\*\*" if 'chave\_pix' in df\_display.columns: df\_display['chave\_pix'] = "Confidencial" if current\_user['perfil'] == 'Visualizador': if 'cpf' in df\_display.columns: df\_display['cpf'] = df\_display['cpf'].apply(mask\_cpf) if 'rg' in df\_display.columns: df\_display['rg'] = "\*\*\*.\*\*\*" if 'pis' in df\_display.columns: df\_display['pis'] = "\*\*\*\*\*\*\*\*\*\*\*" col\_views = ['funcional', 'nome', 'cpf', 'funcao', 'filial', 'status', 'carga\_horaria', 'data\_admissao', 'telefone', 'salario\_base'] valid\_cols = [c for c in col\_views if c in df\_display.columns] st.dataframe(df\_display[valid\_cols], use\_container\_width=True, hide\_index=True) with tab\_novo: if current\_user['perfil'] == 'Visualizador': st.warning("⚠️ Seu perfil é de \*\*Visualizador\*\* e não possui permissão para cadastrar novos colaboradores.") else: st.subheader("Cadastrar Novo Colaborador") with st.form("form\_novo\_colaborador", clear\_on\_submit=True): st.markdown("##### 1\. Informações Contratuais e Empresa") c1, c2, c3, c4 = st.columns(4) f\_funcional = c1.text\_input("Matrícula / Funcional\*") f\_nome = c2.text\_input("Nome Completo\*") f\_filial = c3.selectbox("Filial / Unidade\*", [ "Hospital Barueri / Supera", "Camino School / Supera", "Hospital Cubatão/ Supera", "Fund.Alt.Vent.- FAV/Supera", "Hospital Mandaqui/ Supera", "Hospital Picos / Supera", "Hospital Paulista / Supera", "Hospital Mutinga / Supera", "Singularidades / Supera", "Sede - Supera" ]) f\_status = c4.selectbox("Status Inicial\*", ["Ativo", "Afastado INSS", "Demitido"]) c5, c6, c7, c8 = st.columns(4) f\_funcao = c5.text\_input("Função / Cargo\*", value="AUXILIAR DE COZINHA") f\_cod\_funcao = c6.text\_input("Código Função", value="758") f\_admissao = c7.date\_input("Data de Admissão", value=datetime.date.today()).strftime("%d/%m/%Y") f\_carga\_horaria = c8.text\_input("Carga Horária / Escala", value="07:00-16:48 Seg a Sex") st.markdown("##### 2\. Documentos Pessoais &amp; Dados de Contato") d1, d2, d3, d4, d5 = st.columns(5) f\_cpf = d1.text\_input("CPF\*") f\_data\_nasc = d2.text\_input("Data Nascimento (DD/MM/AAAA)") f\_sexo = d3.selectbox("Sexo", ["F", "M"]) f\_rg = d4.text\_input("RG / Documento") f\_pis = d5.text\_input("PIS") ct1, ct2, ct3 = st.columns(3) f\_naturalidade = ct1.text\_input("Naturalidade") f\_telefone = ct2.text\_input("Telefone de Contato") f\_email = ct3.text\_input("E-mail Profissional/Pessoal") st.markdown("##### 3\. Endereço Residencial") e1, e2, e3, e4 = st.columns([2, 3]) f\_cep = e1.text\_input("CEP") f\_endereco = e2.text\_input("Logradouro / Endereço") f\_numero = e3.text\_input("Número") f\_complemento = e4.text\_input("Complemento") e5, e6, e7 = st.columns(3) f\_bairro = e5.text\_input("Bairro") f\_cidade = e6.text\_input("Cidade / Localidade", value="Barueri") f\_uf = e7.text\_input("UF", value="SP") st.markdown("##### 4\. Dados Financeiros &amp; Benefícios (Privativo RH)") if current\_user['perfil'] == 'Administrador': b1, b2, b3, b4 = st.columns(4) f\_salario = b1.number\_input("Salário Base (R$)", value=1702.47, step=50.0) f\_banco = b2.text\_input("Banco (Código ou Nome)", value="33 - Santander") f\_agencia = b3.text\_input("Agência") f\_conta = b4.text\_input("Conta Corrente/Poupança") p1, p2, p3 = st.columns(3) f\_chave\_pix = p1.text\_input("Chave PIX") f\_vt = p2.selectbox("Vale Transporte?", ["Sim", "Não"]) f\_am = p3.selectbox("Assistência Médica?", ["Sim", "Não"]) else: st.info("ℹ️ Dados financeiros só podem ser inseridos pelo perfil Administrador.") f\_salario, f\_banco, f\_agencia, f\_conta, f\_chave\_pix = 0.0, "", "", "", "" f\_vt, f\_am = "Não", "Não" salvar\_btn = st.form\_submit\_button("💾 Salvar Cadastro de Colaborador", use\_container\_width=True) if salvar\_btn: if not f\_nome or not f\_funcional: st.error("Preencha os campos obrigatórios (Nome e Matrícula/Funcional).") else: conn = get\_connection() c = conn.cursor() try: c.execute(''' INSERT INTO colaboradores ( funcional, nome, cpf, data\_nascimento, sexo, naturalidade, cep, endereco, numero, complemento, bairro, cidade, uf, rg, pis, data\_admissao, cod\_funcao, funcao, filial, status, carga\_horaria, telefone, email, salario\_base, banco, agencia, conta, chave\_pix, vale\_transporte, assistencia\_medica ) VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?) ''', ( f\_funcional, f\_nome, f\_cpf, f\_data\_nasc, f\_sexo, f\_naturalidade, f\_cep, f\_endereco, f\_numero, f\_complemento, f\_bairro, f\_cidade, f\_uf, f\_rg, f\_pis, f\_admissao, f\_cod\_funcao, f\_funcao, f\_filial, f\_status, f\_carga\_horaria, f\_telefone, f\_email, f\_salario, f\_banco, f\_agencia, f\_conta, f\_chave\_pix, f\_vt, f\_am )) conn.commit() st.success(f"Colaborador \*\*{f\_nome}\*\* cadastrado com sucesso!") except sqlite3.IntegrityError: st.error(f"A matrícula/funcional '{f\_funcional}' já existe no banco de dados.") finally: conn.close() with tab\_editar: if current\_user['perfil'] == 'Visualizador': st.warning("⚠️ Seu perfil é de \*\*Visualizador\*\* e não possui permissão para editar registros.") else: st.subheader("Editar Dados de Colaborador") conn = get\_connection() colab\_list = conn.execute("SELECT id, funcional, nome, filial FROM colaboradores").fetchall() conn.close() if colab\_list: options = {f"{c[2]} - {c[3]} ({c[4]})": c for c in colab\_list} selected\_colab = st.selectbox("Selecione o Colaborador para Atualizar:", list(options.keys())) colab\_id = options[selected\_colab] conn = get\_connection() c = conn.cursor() c.execute("SELECT \* FROM colaboradores WHERE id = ?", (colab\_id,)) col\_data = c.fetchone() colnames = [desc for desc in c.description] c\_dict = dict(zip(colnames, col\_data)) conn.close() with st.form("form\_edit\_colaborador"): st.markdown(f"Editando: \*\*{c\_dict['nome']}\*\* (Matrícula: {c\_dict['funcional']})") e\_col1, e\_col2, e\_col3 = st.columns(3) ef\_filial = e\_col1.text\_input("Filial / Unidade", value=c\_dict['filial']) ef\_status = e\_col2.selectbox("Status", ["Ativo", "Demitido", "Afastado INSS"], index=0 if c\_dict['status']=='Ativo' else 1) ef\_funcao = e\_col3.text\_input("Função", value=c\_dict['funcao']) e\_col4, e\_col5, e\_col6 = st.columns(3) ef\_tel = e\_col4.text\_input("Telefone", value=c\_dict['telefone'] or "") ef\_email = e\_col5.text\_input("E-mail", value=c\_dict['email'] or "") ef\_escala = e\_col6.text\_input("Carga Horária", value=c\_dict['carga\_horaria'] or "") if current\_user['perfil'] == 'Administrador': st.markdown("##### Dados Financeiros (Administrador)") ef\_sal1, ef\_sal2, ef\_sal3 = st.columns(3) ef\_salario = ef\_sal1.number\_input("Salário Base", value=float(c\_dict['salario\_base'] or 0.0)) ef\_banco = ef\_sal2.text\_input("Banco", value=c\_dict['banco'] or "") ef\_conta = ef\_sal3.text\_input("Conta", value=c\_dict['conta'] or "") ef\_pix = st.text\_input("Chave PIX", value=c\_dict['chave\_pix'] or "") else: ef\_salario = c\_dict['salario\_base'] ef\_banco = c\_dict['banco'] ef\_conta = c\_dict['conta'] ef\_pix = c\_dict['chave\_pix'] btn\_update = st.form\_submit\_button("🔄 Salvar Alterações", use\_container\_width=True) if btn\_update: conn = get\_connection() c = conn.cursor() c.execute(''' UPDATE colaboradores SET filial = ?, status = ?, funcao = ?, telefone = ?, email = ?, carga\_horaria = ?, salario\_base = ?, banco = ?, conta = ?, chave\_pix = ? WHERE id = ? ''', (ef\_filial, ef\_status, ef\_funcao, ef\_tel, ef\_email, ef\_escala, ef\_salario, ef\_banco, ef\_conta, ef\_pix, colab\_id)) conn.commit() conn.close() st.success("Dados do colaborador atualizados com sucesso!") st.rerun() with tab\_import: if current\_user['perfil'] != 'Administrador': st.warning("⚠️ Apenas administradores podem importar planilhas em massa.") else: st.subheader("📥 Carga em Massa via Planilha (Excel ou CSV)") st.markdown("Envie a sua planilha oficial para cadastrar ou atualizar todos os funcionários do sistema de uma só vez.") uploaded\_file = st.file\_uploader("Selecione o arquivo da planilha (.xlsx, .xls ou .csv):", type=["xlsx", "xls", "csv"]) if uploaded\_file is not None: try: if uploaded\_file.name.endswith('.csv'): try: df\_upload = pd.read\_csv(uploaded\_file, encoding='utf-8') except UnicodeDecodeError: uploaded\_file.seek(0) df\_upload = pd.read\_csv(uploaded\_file, encoding='latin1') else: df\_upload = pd.read\_excel(uploaded\_file) st.success(f"Planilha carregada com sucesso! Total de linhas encontradas: \*\*{len(df\_upload)}\*\*") col\_map = map\_dataframe\_columns(df\_upload) st.markdown("##### Mapeamento Automático de Colunas Identificadas") map\_summary = [{"Coluna na Planilha": k, "Campo no Sistema": v} for k, v in col\_map.items()] st.dataframe(pd.DataFrame(map\_summary), use\_container\_width=True) st.markdown("##### Pré-visualização dos Dados (Primeiras 5 Linhas)") st.dataframe(df\_upload.head(5), use\_container\_width=True) mod\_import = st.radio("Modo de Importação:", [ "Atualizar cadastros existentes e inserir novos (Recomendado)", "Substituir totalmente o banco de dados atual pela planilha" ]) btn\_confirm = st.button("🚀 Confirmar e Importar para o Banco de Dados", type="primary", use\_container\_width=True) if btn\_confirm: conn = get\_connection() c = conn.cursor() if "Substituir totalmente" in mod\_import: c.execute("DELETE FROM colaboradores") conn.commit() importados = 0 atualizados = 0 for \_, row in df\_upload.iterrows(): record = {} for orig\_col, mapped\_field in col\_map.items(): val = row[orig\_col] if pd.isna(val) or val is None: val = "" else: val = str(val).strip() record[mapped\_field] = val funcional = record.get("funcional", "") nome = record.get("nome", "") cpf = record.get("cpf", "") if not nome and not funcional and not cpf: continue if not funcional: if cpf: funcional = ''.join(filter(str.isdigit, str(cpf))) else: funcional = f"AUTO\_{importados+atualizados+1}" filial = record.get("filial", "Hospital Barueri / Supera") status = record.get("status", "Ativo") funcao = record.get("funcao", "AUXILIAR DE COZINHA") salario\_base = float(record.get("salario\_base", 0.0)) if record.get("salario\_base", "") != "" else 0.0 c.execute("SELECT id FROM colaboradores WHERE funcional = ? OR (cpf = ? AND cpf != '')", (funcional, cpf)) existente = c.fetchone() if existente: c.execute(''' UPDATE colaboradores SET nome = COALESCE(NULLIF(?,''), nome), cpf = COALESCE(NULLIF(?,''), cpf), filial = COALESCE(NULLIF(?,''), filial), funcao = COALESCE(NULLIF(?,''), funcao), status = COALESCE(NULLIF(?,''), status), salario\_base = CASE WHEN ? &gt; 0 THEN ? ELSE salario\_base END, banco = COALESCE(NULLIF(?,''), banco), conta = COALESCE(NULLIF(?,''), conta), chave\_pix = COALESCE(NULLIF(?,''), chave\_pix) WHERE id = ? ''', ( nome, cpf, filial, funcao, status, salario\_base, salario\_base, record.get("banco", ""), record.get("conta", ""), record.get("chave\_pix", ""), existente )) atualizados += 1 else: c.execute(''' INSERT INTO colaboradores ( funcional, nome, cpf, filial, funcao, status, data\_admissao, carga\_horaria, telefone, email, salario\_base, banco, agencia, conta, chave\_pix, pis, rg ) VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?) ''', ( funcional, nome, cpf, filial, funcao, status, record.get("data\_admissao", ""), record.get("carga\_horaria", ""), record.get("telefone", ""), record.get("email", ""), salario\_base, record.get("banco", ""), record.get("agencia", ""), record.get("conta", ""), record.get("chave\_pix", ""), record.get("pis", ""), record.get("rg", "") )) importados += 1 conn.commit() conn.close() st.balloons() st.success(f"🎉 Importação Concluída com Sucesso! Novos cadastros: \*\*{importados}\*\* | Registros atualizados: \*\*{atualizados}\*\*") except Exception as e: st.error(f"Erro ao ler a planilha: {str(e)}") # MÓDULO 3: RELATÓRIOS elif menu == "🔍 Pesquisa &amp; Relatórios": st.title("🔍 Busca Avançada e Exportação de Relatórios") st.subheader("Filtros de Busca") q\_col1, q\_col2, q\_col3 = st.columns(3) busca\_nome = q\_col1.text\_input("Buscar por Nome ou CPF:") busca\_filial = q\_col2.selectbox("Unidade / Filial:", ["Todas"] + [ "Hospital Barueri / Supera", "Camino School / Supera", "Hospital Cubatão/ Supera", "Fund.Alt.Vent.- FAV/Supera", "Hospital Mandaqui/ Supera", "Hospital Picos / Supera", "Sede - Supera" ]) busca\_status = q\_col3.selectbox("Status do Funcionário:", ["Todos", "Ativo", "Demitido", "Afastado INSS"]) conn = get\_connection() query = "SELECT \* FROM colaboradores WHERE 1=1" params = [] if busca\_nome: query += " AND (nome LIKE ? OR cpf LIKE ? OR funcional LIKE ?)" params.extend([f"%{busca\_nome}%", f"%{busca\_nome}%", f"%{busca\_nome}%"]) if busca\_filial != "Todas": query += " AND filial = ?" params.append(busca\_filial) if busca\_status != "Todos": query += " AND status = ?" params.append(busca\_status) df\_result = pd.read\_sql\_query(query, conn, params=params) conn.close() if current\_user['perfil'] != 'Administrador': if 'salario\_base' in df\_result.columns: df\_result['salario\_base'] = None if 'chave\_pix' in df\_result.columns: df\_result['chave\_pix'] = "Restrito" if current\_user['perfil'] == 'Visualizador': if 'cpf' in df\_result.columns: df\_result['cpf'] = df\_result['cpf'].apply(mask\_cpf) st.subheader(f"Resultados Encontrados ({len(df\_result)})") st.dataframe(df\_result, use\_container\_width=True) st.divider() csv\_data = df\_result.to\_csv(index=False).encode('utf-8') st.download\_button( label="📥 Baixar Relatório em CSV (Excel)", data=csv\_data, file\_name=f"relatorio\_colaboradores\_supera\_{datetime.date.today().strftime('%Y%m%d')}.csv", mime="text/csv", use\_container\_width=True ) # MÓDULO 4: GERENCIAR USUÁRIOS elif menu == "⚙️ Gerenciar Usuários": st.title("⚙️ Controle de Usuários e Permissões de Acesso") if current\_user['perfil'] != 'Administrador': st.error("⛔ Apenas usuários com perfil \*\*Administrador\*\* podem acessar a gestão de permissões.") else: st.subheader("Usuários Cadastrados no Sistema") conn = get\_connection() df\_users = pd.read\_sql\_query("SELECT id, nome, email, perfil, unidade FROM usuarios", conn) conn.close() st.dataframe(df\_users, use\_container\_width=True) st.divider() st.subheader("➕ Adicionar Novo Usuário do Sistema") with st.form("form\_novo\_usuario"): u\_nome = st.text\_input("Nome do Usuário") u\_email = st.text\_input("E-mail corporativo") u\_senha = st.text\_input("Senha Inicial", type="password") u\_perfil = st.selectbox("Perfil de Permissão", ["Administrador", "Editor", "Visualizador"]) u\_unidade = st.selectbox("Restrição de Filial/Unidade", ["Todas", "Hospital Barueri / Supera", "Camino School / Supera", "Hospital Cubatão/ Supera", "Fund.Alt.Vent.- FAV/Supera"]) submit\_user = st.form\_submit\_button("Criar Usuário") if submit\_user: if not u\_nome or not u\_email or not u\_senha: st.error("Preencha todos os campos do novo usuário.") else: conn = get\_connection() c = conn.cursor() try: c.execute("INSERT INTO usuarios (nome, email, senha\_hash, perfil, unidade) VALUES (?,?,?,?,?)", (u\_nome, u\_email, make\_hashes(u\_senha), u\_perfil, u\_unidade)) conn.commit() st.success(f"Usuário \*\*{u\_nome}\*\* criado com sucesso!") st.rerun() except sqlite3.IntegrityError: st.error(f"O e-mail '{u\_email}' já está cadastrado.") finally: conn.close()
